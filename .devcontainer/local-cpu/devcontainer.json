{
  "name": "Python CPU (Local)",
  "dockerComposeFile": [
    "../../docker-compose.yml"
  ],
  "service": "app",
  "workspaceFolder": "/app",
  
  // Instala extensões úteis automaticamente dentro do container
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  }
}